class DataBase {
   constructor(data, models) {
      this.data = data;
      this.models = models;
   }

   // findOne(model, id) {}

   // findAll(model) {}

   // create(model, data) {}

   // edit(model, id, newData) {}

   // delete(model, id) {}
}

module.exports = DataBase;
